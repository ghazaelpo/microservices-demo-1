name: 'Building Recommendation service image'

on:
  push:
    branches:
    - master
    paths:
      - "src/recommendationservice/"

jobs:
  Integration:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: "Login to Azure CLI"
        uses: azure/login@v1  
        with:
          creds: ${{ secrets.AZ_CRED }}

      - name: Login to ACR
        uses: azure/docker-login@v1
        with:
          login-server: boutiqueaksacr.azurecr.io
          username: ${{ secrets.REGISTRY_USERNAME }}
          password: ${{ secrets.REGISTRY_PASSWORD }}

      - name: Delete image 
        run: |
          az acr repository delete --name boutiqueaksacr --image recommendationservice:v1
        continue-on-error: true

      - name: Building Recommendation service
        run: |
          cd src/recommendationservice/
          docker build -t boutiqueaksacr.azurecr.io/recommendationservice:v1 .
          docker push boutiqueaksacr.azurecr.io/recommendationservice:v1
