name: 'Building shipping service image'

on:
  push:
    branches:
    - master
    paths:
      - "src/shipping/Dockerfile"

jobs:
  Integration:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: "Login to Azure CLI"
        uses: azure/login@v1  
        with:
          creds: ${{ secrets.AZ_CRED }}

      - name: Login to ACR
        uses: azure/docker-login@v1
        with:
          login-server: boutiqueaksacr.azurecr.io
          username: ${{ secrets.REGISTRY_USERNAME }}
          password: ${{ secrets.REGISTRY_PASSWORD }}

      - name: Delete image 
        run: |
          az acr repository delete --name boutiqueaksacr --image shippingservice:v1

      - name: Building Shipping Service
        run: |
          cd src/shippingservice/
          docker build -t boutiqueaksacr.azurecr.io/shippingservice:v1 .
          docker push boutiqueaksacr.azurecr.io/shippingservice:v1
